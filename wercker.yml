box: wercker/default
build:
    steps:
        - shellcheck:
                files: "*.sh"
        - script:
                name: test.sh
                code: "./run.sh"
    after-steps:
        - turistforeningen/slack-notifier:
                url: $SLACK_WEBHOOK_URL
